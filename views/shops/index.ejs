<h1>All Shops</h1>

<div class="row">
    <% shops.forEach(s => {%>
        <div class="col s3">
            <h2>
                <a href="/shops/<%=s.id%>">
                    <%= s.name %>
                </a>
            </h2>
        </div>
    <% }) %>
</div>